# Changelog

Written centrally at release time by the maintainer. A code PR never edits this file.

## Unreleased

- M0 — scaffold: `defineStack()` entry point, CI gates, `DESIGN.md`, backlog cards, and the four
  configuration-domain objects (`clm_contract_type`, `clm_clause`, `clm_approval_rule`, `clm_party`).

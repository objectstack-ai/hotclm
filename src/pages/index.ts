// The record pages of DESIGN.md §05: this barrel is spread into
// `defineStack({ pages })` via `Object.values()`, so anything exported here is
// registered as a Page.
export { ContractDetailPage } from './contract_detail.page.js';
